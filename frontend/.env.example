# Local development uses Vite proxy to forward /api to backend (http://127.0.0.1:8000)
# In cloud deployment (e.g. Vercel), point to your hosted FastAPI backend (e.g. https://agrinex-api.onrender.com)
VITE_API_URL=
